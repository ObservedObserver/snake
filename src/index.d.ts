type position = [number, number];

type SnakeSpace = Array<position>;

type BBox = [[number, number], [number, number]];

type mode = 'easy' | 'medium' | 'hard' | 'crazy';

export { position, SnakeSpace, BBox, Mode };